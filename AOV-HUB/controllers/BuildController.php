<?php

/**
 * BuildController — Build đề xuất
 * Phụ trách: Người 3
 */
class BuildController
{
    // index.php?action=build&hero_id=1
    public function showByHero()
    {
        // TODO:
        //  - $heroId = $_GET['hero_id']
        //  - $build  = (new BuildModel)->getByHero($heroId)
        //  - $items  = (new ItemModel)->findMany($build['items'])
        //  - $view   = 'builds/show';
    }
}
