/**
 * storage.js - Đọc/ghi LocalStorage
 * Phụ trách: Người 4
 *
 * Key: aov_favorites / aov_history / aov_compare
 * TODO:
 *  getStore(key) / setStore(key, value)   - có JSON.parse, JSON.stringify, try-catch
 *  addFavorite(type, id) / removeFavorite(type, id) / isFavorite(type, id) / getFavorites(type)
 *  addHistory(id)                         - tối đa 10, không trùng
 *  getHistory() / clearHistory()
 *  addCompare(id)                         - tối đa 2 tướng
 *  getCompare() / clearCompare()
 */
